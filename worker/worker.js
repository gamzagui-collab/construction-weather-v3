export default { async fetch(){ return new Response(JSON.stringify({ok:true,message:'기존 Cloudflare Worker forecast 코드를 유지해도 됩니다. v6.0 Phase1은 프론트 모듈 구조 리팩터링입니다.'}),{headers:{'Content-Type':'application/json; charset=utf-8'}}); } };
