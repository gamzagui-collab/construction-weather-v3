const WORKER_BASE_URL = "https://weather-proxy.gamzagui.workers.dev";
async function searchRegionByWorker(keyword){const url=`${WORKER_BASE_URL}/geocode?q=${encodeURIComponent(keyword)}`;const res=await fetch(url,{cache:"no-store"});if(!res.ok)throw new Error(`지역 검색 실패: ${res.status}`);return await res.json();}
async function reverseGeocodeByWorker(lat,lon){const url=`${WORKER_BASE_URL}/reverse?lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}`;const res=await fetch(url,{cache:"no-store"});if(!res.ok)throw new Error(`위치명 조회 실패: ${res.status}`);return await res.json();}
async function fetchForecastFromWorker({lat,lon,name}){const url=`${WORKER_BASE_URL}/forecast?lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lon)}&name=${encodeURIComponent(name||"선택 위치")}`;const res=await fetch(url,{cache:"no-store"});if(!res.ok){const text=await res.text();throw new Error(`예보 조회 실패: ${res.status} ${text}`);}const json=await res.json();if(!json.ok)throw new Error(json.message||"Worker 예보 응답 오류");return json;}
function parseCoordInput(input){const value=input.trim();if(!value)return null;const m=value.match(/^(-?\d+(\.\d+)?),\s*(-?\d+(\.\d+)?)$/);if(!m)return null;return {name:"직접 입력 좌표",lat:Number(m[1]),lon:Number(m[3])};}
function getRegionFromLocalKeyword(keyword){const local=findLocalRegionByKeyword(keyword);if(!local)return null;return {name:local.name,lat:local.lat,lon:local.lon,key:local.key,source:"local"};}
function normalizeGeocodeResult(item){return {name:item.name,lat:item.lat,lon:item.lon,key:`geo_${item.lat}_${item.lon}`,source:"geocode"};}
