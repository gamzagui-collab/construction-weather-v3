import { checklistForTrade } from '../database/checklists.js';
export function renderTradeTasks(trades){ const el=document.getElementById('tradeTasks'); if(!el)return; const tasks=[...new Set(trades.flatMap(checklistForTrade))]; el.innerHTML=`<div class="task-card"><h4>공정별 통합 체크리스트 (${tasks.length})</h4>${tasks.map(t=>`<label><input type="checkbox"> ${t}</label>`).join('')}</div>`; }
export function renderRoleTasks(tasks){ const el=document.getElementById('roleTasks'); if(!el)return; el.innerHTML=`<div class="task-card"><h4>역할별 오늘 할 일</h4>${tasks.map(t=>`<label><input class="role-task-check" type="checkbox"> ${t}</label>`).join('')}</div>`; document.getElementById('checkAllRoleTasks')?.addEventListener('click',()=>document.querySelectorAll('.role-task-check').forEach(c=>c.checked=true)); document.getElementById('clearRoleTasks')?.addEventListener('click',()=>document.querySelectorAll('.role-task-check').forEach(c=>c.checked=false)); }
